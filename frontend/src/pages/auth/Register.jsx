import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{ArrowLeft,Car,UserRound}from'lucide-react';import{Brand,Button,useApp,Notice,Badge}from'../../shared';
export default function Register(){const{register}=useApp();const nav=useNavigate();const[role,setRole]=useState('rider');const[form,setForm]=useState({fullName:'',phone:'',email:'',password:''});const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setError('');setBusy(true);try{const u=await register({...form,role});nav(u.role==='driver'?'/driver':'/home',{replace:true})}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="auth-page"><div className="auth-card wide"><Link to="/welcome" className="back"><ArrowLeft size={17}/> Back</Link><Brand/><div className="auth-head"><h1>Create your account</h1><p>Join Kaduna Only in a few simple steps.</p></div>{error&&<Notice title="Registration failed" text={error} tone="danger"/>}<div className="role-picker"><button className={role==='rider'?'selected':''} onClick={()=>setRole('rider')}><UserRound/>Rider<Badge>Passenger</Badge></button><button className={role==='driver'?'selected':''} onClick={()=>setRole('driver')}><Car/>Driver<Badge>Earn with us</Badge></button></div><form onSubmit={submit} className="form"><label>Full name<input value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} required/></label><label>Phone number<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} required/></label><label>Email <span>(optional)</span><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" minLength="8" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="At least 8 characters" required/></label><Button type="submit" full disabled={busy}>{busy?'Creating account…':'Create Account'}</Button></form><p className="auth-switch">Already registered? <Link to="/login">Sign in</Link></p></div></div>}

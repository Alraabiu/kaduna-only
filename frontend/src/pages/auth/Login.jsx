import React,{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{Eye,EyeOff,ArrowLeft}from'lucide-react';import{Brand,Button,useApp,Notice}from'../../shared';
export default function Login(){const{login}=useApp();const nav=useNavigate();const[form,setForm]=useState({phone:'',password:''});const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setError('');setBusy(true);try{const u=await login(form);nav(u.role==='admin'?'/admin':u.role==='driver'?'/driver':'/home',{replace:true})}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="auth-page"><div className="auth-card"><Link to="/welcome" className="back"><ArrowLeft size={17}/> Back</Link><Brand/><div className="auth-head"><h1>Welcome back</h1><p>Sign in to continue to Kaduna Only.</p></div>{error&&<Notice title="Unable to sign in" text={error} tone="danger"/>}<form onSubmit={submit} className="form"><label>Phone number<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="08012345678" autoComplete="tel" required/></label><label>Password<div className="password"><input type={show?'text':'password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Your password" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><Button type="submit" full disabled={busy}>{busy?'Signing in…':'Sign In'}</Button></form><p className="auth-switch">Don't have an account? <Link to="/register">Create one</Link></p></div></div>}

const r=require('express').Router();const c=require('../controllers/walletController');const {requireAuth}=require('../middleware/auth');
r.use(requireAuth);r.get('/',c.me);r.get('/transactions',c.transactions);r.get('/payments',c.paymentHistory);r.post('/paystack/initialize',c.initializePaystack);r.post('/paystack/verify',c.verifyPaystack);r.post('/pin',c.setPin);r.post('/transfer',c.transfer);module.exports=r;
